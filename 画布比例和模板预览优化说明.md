# 画布比例和模板预览优化说明

## 功能1: 画布比例选择 ✅

### 功能描述
在布局选择页面添加画布比例选择器，支持多种常用比例，包括社交媒体、海报、纸张等常见尺寸。

### 支持的比例

| 比例 | 说明 | 适用场景 |
|------|------|----------|
| **1:1 正方形** | 正方形画布 | Instagram帖子、微信朋友圈 |
| **3:4 竖版** | 竖版照片 | 手机壁纸、竖版海报 |
| **4:3 横版** | 横版照片 | 传统相机比例 |
| **9:16 手机竖屏** | 手机竖屏 | 抖音、快手、Instagram Story |
| **16:9 手机横屏** | 手机横屏 | YouTube、B站视频封面 |
| **10:16 竖版海报** | 竖版海报 | 宣传海报、易拉宝 |
| **16:10 横版海报** | 横版海报 | 横幅、展板 |
| **A4 纸张** | A4纸张竖向 | 打印文档、传单 |
| **A4 横向** | A4纸张横向 | 横向文档、证书 |

### 用户界面

**位置**: ⭐ **编辑页面的样式控制区域**（Canvas下方，第一个控制项）

**为什么放在编辑页面？**
- ✅ 用户可以**实时看到**比例变化后的Canvas效果
- ✅ 更符合用户的操作习惯（调整样式时一起调整比例）
- ✅ 避免在布局选择页面分散注意力

**样式**:
- 横向滚动列表（紧凑型芯片设计）
- 圆角胶囊按钮（比布局选择页面更小巧）
- 选中状态：紫色渐变背景 + 白色文字
- 未选中状态：灰色背景 + 黑色文字

**交互**:
- 点击切换比例
- 显示Toast提示: "已切换到 XXX"
- **立即重新绘制Canvas**，用户可以实时看到效果

### 代码实现

#### 1. 数据定义 (`pages/collage/collage.js` Line 30-46)

```javascript
aspectRatio: '1:1',
aspectRatios: [
  { label: '1:1 正方形', value: '1:1', width: 1, height: 1 },
  { label: '3:4 竖版', value: '3:4', width: 3, height: 4 },
  { label: '4:3 横版', value: '4:3', width: 4, height: 3 },
  { label: '9:16 手机竖屏', value: '9:16', width: 9, height: 16 },
  { label: '16:9 手机横屏', value: '16:9', width: 16, height: 9 },
  { label: '10:16 竖版海报', value: '10:16', width: 10, height: 16 },
  { label: '16:10 横版海报', value: '16:10', width: 16, height: 10 },
  { label: 'A4 纸张', value: 'A4', width: 210, height: 297 },
  { label: 'A4 横向', value: 'A4-H', width: 297, height: 210 }
]
```

#### 2. 事件处理 (`pages/collage/collage.js` Line 468-502)

```javascript
onAspectRatioChange (e) {
  const ratioValue = e.currentTarget?.dataset?.ratio || e.detail?.value;
  
  // 如果是从dataset获取的值(点击事件)
  if (typeof ratioValue === 'string') {
    const ratio = this.data.aspectRatios.find(r => r.value === ratioValue);
    if (ratio) {
      this.setData({
        aspectRatio: ratio.value
      });
      this.calculateCanvasSize(ratio.width, ratio.height);
      
      wx.showToast({
        title: `已切换到 ${ratio.label}`,
        icon: 'success',
        duration: 1500
      });
      
      console.log('切换画布比例:', ratio.label);
    }
  }
  
  // 如果已经选择了布局,重新绘制
  if (this.data.currentLayoutTemplate) {
    this.updateCanvas();
  }
}
```

#### 3. UI界面 (`pages/collage/collage.wxml` Line 147-163)

**位置**: 编辑页面 → 样式控制区域 → 第一个控制项

```xml
<!-- 画布比例 -->
<view class="control-item control-item-full">
  <text class="control-label">画布比例</text>
  <scroll-view class="aspect-ratio-scroll-inline" scroll-x>
    <view class="aspect-ratio-list-inline">
      <view
        class="aspect-ratio-chip {{aspectRatio === item.value ? 'active' : ''}}"
        wx:for="{{aspectRatios}}"
        wx:key="value"
        data-ratio="{{item.value}}"
        bindtap="onAspectRatioChange"
      >
        <text class="ratio-chip-label">{{item.label}}</text>
      </view>
    </view>
  </scroll-view>
</view>
```

#### 4. 样式 (`pages/collage/collage.wxss` Line 105-149)

**编辑页面内联样式** - 更紧凑的芯片设计：

```css
/* 画布比例选择 - 编辑页面内联样式 */
.control-item-full {
  flex-direction: column;
  align-items: flex-start !important;
  gap: 8px;
}

.aspect-ratio-scroll-inline {
  width: 100%;
  white-space: nowrap;
}

.aspect-ratio-list-inline {
  display: inline-flex;
  gap: 6px;
  padding: 2px 0;
}

.aspect-ratio-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 12px;  /* 更紧凑 */
  background: #f5f5f5;
  border: 1.5px solid #e0e0e0;
  border-radius: 16px;  /* 更小的圆角 */
  transition: all 0.2s;
  flex-shrink: 0;
}

.aspect-ratio-chip.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-color: #667eea;
  box-shadow: 0 2px 6px rgba(102, 126, 234, 0.25);
}

.ratio-chip-label {
  font-size: 12px;  /* 更小的字体 */
  color: #333;
  font-weight: 500;
  white-space: nowrap;
}

.aspect-ratio-chip.active .ratio-chip-label {
  color: white;
}
```

---

## 功能2: 模板预览可视化 ✅

### 功能描述
将原来的文字图标预览改为可视化的布局结构预览，用户可以直观地看到每个模板的图片排列方式。

### 预览效果

**网格布局**:
- 使用 CSS Grid 显示网格结构
- 每个格子用浅紫色填充
- 格子之间有间隙
- 选中状态：深紫色渐变

**自定义布局**:
- 使用绝对定位显示每个图片位置
- 根据 positions 数组计算位置和尺寸
- 每个位置用浅紫色矩形表示
- 选中状态：深紫色渐变

### 视觉对比

**之前**: 
```
┌─────────┐
│    ▦    │  ← 只有一个文字图标
│  2×2网格 │
│   4张   │
└─────────┘
```

**现在**:
```
┌─────────┐
│ ┌─┬─┐  │  ← 可视化显示2×2网格
│ ├─┼─┤  │
│ └─┴─┘  │
│  2×2网格 │
│   4张   │
└─────────┘
```

### 代码实现

#### 1. WXML模板 (`pages/collage/collage.wxml` Line 67-102)

```xml
<view class="template-preview-visual">
  <!-- 网格布局预览 -->
  <view 
    wx:if="{{item.type === 'grid'}}" 
    class="grid-preview" 
    style="grid-template-rows: repeat({{item.rows}}, 1fr); 
           grid-template-columns: repeat({{item.cols}}, 1fr);"
  >
    <view 
      class="grid-cell" 
      wx:for="{{item.imageCount}}" 
      wx:for-item="cellIndex" 
      wx:key="cellIndex"
    ></view>
  </view>
  
  <!-- 自定义布局预览 -->
  <view wx:else class="custom-preview">
    <view 
      class="custom-cell" 
      wx:for="{{item.positions}}" 
      wx:for-item="pos" 
      wx:key="index"
      style="left: {{pos.x * 100}}%; 
             top: {{pos.y * 100}}%; 
             width: {{pos.width * 100}}%; 
             height: {{pos.height * 100}}%;"
    ></view>
  </view>
</view>
```

#### 2. 样式 (`pages/collage/collage.wxss` Line 214-268)

```css
/* 可视化预览容器 */
.template-preview-visual {
  width: 80px;
  height: 80px;
  background: #f8f8f8;
  border-radius: 8px;
  padding: 6px;
  position: relative;
  overflow: hidden;
}

.layout-template-card.selected .template-preview-visual {
  background: white;
  box-shadow: 0 0 0 2px #667eea inset;
}

/* 网格布局预览 */
.grid-preview {
  width: 100%;
  height: 100%;
  display: grid;
  gap: 3px;
}

.grid-cell {
  background: linear-gradient(135deg, #a8b5ff 0%, #c5d0ff 100%);
  border-radius: 3px;
  border: 1px solid #8899ff;
}

.layout-template-card.selected .grid-cell {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-color: #5566dd;
}

/* 自定义布局预览 */
.custom-preview {
  width: 100%;
  height: 100%;
  position: relative;
}

.custom-cell {
  position: absolute;
  background: linear-gradient(135deg, #a8b5ff 0%, #c5d0ff 100%);
  border-radius: 3px;
  border: 1px solid #8899ff;
  box-sizing: border-box;
}

.layout-template-card.selected .custom-cell {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-color: #5566dd;
}
```

---

## 测试步骤

### 测试功能1: 画布比例选择

1. **选择布局并进入编辑页面**
   - 选择任意布局模板
   - 进入编辑页面（有Canvas的页面）

2. **查看画布比例选择器**
   - 应该在Canvas下方的"样式"区域
   - 第一个控制项就是"画布比例"
   - 显示9个比例选项（横向滚动）
   - 默认选中"1:1 正方形"（紫色背景）

3. **实时切换比例**
   - 点击"3:4 竖版"
   - 应该显示Toast: "已切换到 3:4 竖版"
   - 该选项变为紫色背景
   - **Canvas立即变为竖版比例** ⭐

4. **上传图片后切换比例**
   - 上传几张图片
   - 切换到"9:16 手机竖屏"
   - **Canvas和图片立即按新比例重新绘制** ⭐
   - 图片布局保持不变，只是画布比例改变

5. **测试不同比例**
   - 依次测试：1:1、3:4、16:9、A4等
   - 每次切换都应该能实时看到Canvas变化

### 测试功能2: 模板预览可视化

1. **进入布局选择页面**
2. **查看模板卡片**
   - 每个模板应该显示可视化的布局结构
   - 网格布局：显示网格格子
   - 自定义布局：显示不规则的矩形排列
3. **点击不同分类**
   - 点击"2张"分类
   - 应该看到横向排列、竖向排列等不同的可视化预览
4. **选中模板**
   - 点击一个模板
   - 预览图应该变为深紫色
   - 边框变为紫色

---

## 用户体验提升

### 功能1: 画布比例
- ✅ **更多选择**: 从6种比例增加到9种
- ✅ **清晰标注**: 每个比例都有说明（如"手机竖屏"、"A4纸张"）
- ✅ **即时反馈**: 切换时显示Toast提示
- ✅ **实时预览**: ⭐ **放在编辑页面，切换比例立即看到Canvas变化**
- ✅ **智能重绘**: 自动重新绘制Canvas和所有图片

### 功能2: 模板预览
- ✅ **直观可视**: 一眼就能看出布局结构
- ✅ **美观设计**: 渐变色彩，圆角设计
- ✅ **选中高亮**: 选中状态更明显
- ✅ **支持所有布局**: 网格和自定义布局都能正确显示

---

## 技术亮点

### 1. 动态Grid布局
使用 `grid-template-rows` 和 `grid-template-columns` 动态生成网格：
```css
grid-template-rows: repeat({{item.rows}}, 1fr);
grid-template-columns: repeat({{item.cols}}, 1fr);
```

### 2. 百分比定位
自定义布局使用百分比计算位置：
```css
left: {{pos.x * 100}}%;
top: {{pos.y * 100}}%;
width: {{pos.width * 100}}%;
height: {{pos.height * 100}}%;
```

### 3. 渐变色彩
使用线性渐变增强视觉效果：
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

---

## 注意事项

1. ⚠️ **比例切换**: 切换比例会改变Canvas尺寸，可能影响已有图片的显示
2. ⚠️ **自定义布局**: positions数组必须正确定义，否则预览可能不准确
3. ✅ **性能优化**: 使用CSS而非Canvas绘制预览，性能更好
4. ✅ **响应式**: 预览图大小固定为80×80px，适配各种屏幕

---

## 总结

这两个功能极大地提升了用户体验：

1. **画布比例选择**: 让用户可以根据不同场景选择合适的画布尺寸
2. **模板预览可视化**: 让用户一眼就能看出每个模板的布局结构

两个功能都采用了现代化的UI设计，与整体风格保持一致！ 🎉

